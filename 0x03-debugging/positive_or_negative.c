#include "main.h"
/**
 * Positive_or_Negtaive
 * Checks for positive or negative
 * * Return: 0 always
 */

void postive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);
}

